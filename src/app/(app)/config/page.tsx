import { PagePlaceholder } from "@/components/page-placeholder";

export default function ConfigPage() {
  return (
    <PagePlaceholder
      title="Config"
      description="Perfil, cuenta, hábitos, objetivos, privacidad y conexión con Strava."
    />
  );
}
